import React from 'react'
import PropTypes from 'prop-types'

interface BtnProps {
    title:string
 }
function NextButton(props:BtnProps) {
  return (
    <div>NextButton</div>
  )
}

NextButton.propTypes = {}

export default NextButton
